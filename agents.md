# 开发宪法：AI 与开发者协作规则

版本：0.1.0｜日期：2026-09-16｜适用：本项目所有人工开发、AI 会话及并行工作者。

本文件名按用户要求为小写 `agents.md`。不要假定所有编辑器会自动加载它。每次会话启动必须显式阅读本文件；在 VS Code 的项目指令或首条提示中引用它。六份文件复制到项目根目录后才构成该项目的治理文档。

## 1. 权威顺序与范围

用户最新明确指示 > requirements.md 产品规则 > 本文件工程约束 > tasks.md 任务计划 > current_task.md 当前切片 > handoff.md 状态记录 > changelog.md 已完成历史。状态日志不能反向改写需求。

唯一授权开发范围是当前切片明确列出的任务及可写文件。发现必要依赖缺失时登记阻塞或提出最小调整；不借“顺手完善”加入新平台、云服务、自由 AI 问答、收费系统、视频或 OBS 改造。

本文不增加系统级工具权限。用户明确授权的常规可逆工作继续执行；只有真实缺少信息/权限且影响当前动作时才询问。修改产品业务规则或实施新的外部行为必须由用户明确决定。

## 2. 新会话恢复流程

依次阅读 `agents.md → current_task.md → handoff.md → tasks.md → requirements.md 的相关章节 → changelog.md 最近记录`。首次进入项目还需通读 requirements.md。不要只凭旧聊天或 handoff 断言现状。

然后执行：

1. 确认实际仓库根目录、分支、HEAD、`git status --short`、`git worktree list`，以及当前环境工具可用性。
2. 对照 handoff 中记录的状态；保留所有不属于自己的改动。有重叠/不一致时先解释，不重置用户修改。
3. 检查 current_task 的状态、依赖与任务所有者。若切片仍在执行，沿用现有范围；若为 DONE，按批准任务依赖选择下一个最小 READY 切片并更新文档后开始。
4. 如果没有 Git 仓库，不假造分支/提交；在允许范围内完成工具链检查和初始化建议。创建仓库等动作按当时用户授权处理。
5. 先确认已有产物，避免重复生成脚手架、二次导入数据库或重跑有副作用的真实平台操作。

开始工具操作前给出一句当前目标；长任务每约 60 秒提供有意义的进展。只做文档任务时不启动实现。

## 3. 架构基线

### 3.1 默认技术选择

目标 Windows 10/11 x64；C#、.NET 10 LTS、WPF + MVVM、SQLite、NAudio/WASAPI，按必要性使用 FFmpeg 预处理。依赖版本在工具链任务中验证并固定，不默认安装最新版所有组件。若目标工具链无法支持此基线，先记录原因和替代建议，不静默改技术栈。

VS Code 是编辑/构建/调试环境；不用可视化设计器作为必要前提。OBS 或 TikTok LIVE Studio 独立运行，本工具向用户选择的音频设备输出。虚拟声卡可以由用户自行配置；不安装自研驱动、不嵌入 libobs，也不强制要求 OBS WebSocket 才能播放音频。

### 3.2 项目依赖方向

```text
Desktop (组合根、WPF/MVVM)
  ├─ Application (用例、调度与端口)
  │    └─ Domain (事件、规则、计划、不变量)
  └─ Infrastructure (平台、本地TTS、音频、SQLite)
       ├─ Application
       └─ Domain

Tests → 对应被测项目
Domain 不依赖 UI、数据库、NAudio、TikTok 或网络。
```

建议路径是后续开发目标，不代表当前已有文件：

```text
src/TikTokAudio.Domain/
src/TikTokAudio.Application/
src/TikTokAudio.Infrastructure/LiveSources/
src/TikTokAudio.Infrastructure/Tts/
src/TikTokAudio.Infrastructure/Audio/
src/TikTokAudio.Infrastructure/Control/
src/TikTokAudio.Infrastructure/Persistence/
src/TikTokAudio.Desktop/
tests/TikTokAudio.Domain.Tests/
tests/TikTokAudio.Application.Tests/
tests/TikTokAudio.Integration.Tests/
```

只在切片需要时创建项目/目录，禁止提前建一堆空服务。使用清晰的小类，避免全局 Service Locator、万能 Manager 和跨层静态状态。

### 3.3 必须冻结的边界契约

T020 由整合负责人冻结以下接口、模型和语义后再并行实现。以下名称是设计约束，具体 C# 签名以那次契约成果为准，不让各分支各自定义：

| 契约 | 责任 |
|---|---|
| ILiveEventSource | 连接、能力、按顺序输出规范事件、断连；不直接播音频或操作商品 |
| ILiveRoomController | 展示商品、读取可见状态、发送文字；返回 Confirmed/Rejected/Unknown/Unsupported |
| ITtsProvider | 健康、音色/参数能力、异步生成、取消；返回可解码本地资产及实际引擎标识 |
| IAudioOutput | 单一主语音流播放/暂停/恢复/停止及源游标；环境音独立混音 |
| IPlaybackPlanner | 选择下个基础片段、恢复检查点、发出一次性的产品/标记边界 |
| IClock / IRandomSource | 可控时钟与随机，支持无真实等待的竞态测试 |
| 状态仓储端口 | 规则、计划、会话、去重、音频缓存元数据与动作账本持久化 |

并行工作者不单独修改共享接口、领域枚举、解决方案文件、公共包版本或数据库迁移。需要改契约先给整合负责人最小差异与受影响调用者，统一更新契约和任务后继续。

## 4. 不可破坏的业务不变量

- 一条主语音输出；TTS 和预制模式互斥；插播不移动基础计划游标。
- 音频准备好才打断基础；互动之间不互相打断。
- 欢迎/关注的用户业务去重与原始事件去重分开；成功结束才计已完成。
- K/V/W 由应用规则计时，不能解释为人数阈值，也不能从未知配置单位猜测秒数。
- 产品链接只由唯一场控控制器提交；ProductEpoch 消除旧定时任务；30 秒从展示确认成功开始。
- n-1 触发链接 n；n-2 及后续组不改产品；互动插播不改产品；`@[n]` 不朗读、不由远程用户内容执行。
- 用户 ID 缺失、事件不完整、操作结果未知都显式降级，不能伪造平台成功。
- TTS 仅本机；不自动将稿件、评论或昵称发送给云端。
- 崩溃后不自行恢复外部副作用；断线不补发积压文字；停止后旧任务不得复活。

## 5. 编码规范

- nullable 开启；异步 I/O 使用 async/await 和 CancellationToken，避免 `.Result`、`.Wait()` 和 UI 线程阻塞。
- Core 使用不可变数据或清楚的状态所有者；不要靠多个 Timer 共同修改音频/商品目标。推荐单消费者命令队列串行管理播放和副作用状态。
- 时长用 TimeSpan，存储时间用 DateTimeOffset UTC；播放进度以源采样位置为准；冷却计时不依赖可回拨墙钟。
- 随机源可注入，记录当前效果/洗牌状态，禁止用每次 new Random 造成不可测试选择。
- 队列、日志、缓存、重试必须有边界；取消和超时是正常结果，异常不可吞掉。
- 结果类型明确区分成功、失败、未知、取消、不支持；网络返回/页面点击不是业务成功证据。
- 音频缓存临时写入后验证再提交；路径限制在选定素材/缓存目录，不允许稿件标记任意访问文件或执行命令。
- 中文产品文案集中维护；代码标识符清晰英文；对越南语 Unicode 使用一致规范化，不默认去除重音。
- 保持文件/模块大小适中；只对难以看懂的业务原因写注释，避免测试只复述实现。
- 凭据、Cookie、原始用户信息不打印；示例使用合成账号和评论。资料包里的日志/浏览器配置不得复制到项目。

## 6. 并行开发协议

“多线程开发”指多个受控工作者同时实现互不重叠模块，不等于多条语音同时播放，也不允许一个会话擅自扩张任务。

### 6.1 一个切片，多个子任务

`current_task.md` 永远只有一个全局活动切片。并行时，切片包含一张子任务分配表；每个工作者只领取其中一行，不另建第二个 current_task 文件。

全局只能有一个整合负责人（Integrator），有权修改六份治理文档、共享契约、项目配置和迁移顺序。工作者只改其分配文件和专属测试。源码可以多分支，但产品真相和全局状态有单一维护者。

### 6.2 分配与隔离

1. 负责人验证依赖 DONE、接口已冻结，将当前切片设为 ACTIVE，逐行登记 Owner/Task/Branch/Worktree/AllowedPaths/BaseCommit。
2. 每个工作者使用独立 Git worktree/分支，分支约定 `task/<task-id>-<short-name>`。未确认 Git 状态前不要创建或重用目录。
3. 文件所有权写成明确路径前缀；不能两个工作者同时写同一个文件、csproj、共享测试夹或迁移文件。
4. 若工具只能共享目录，负责人进一步分配互斥文件，禁止同时运行会修改共享生成文件/数据库/音频设备的任务；无法隔离则顺序执行。
5. 并行人数按实际环境，建议负责人 1 + 工作者最多 3。用户没有要求实际并行执行时，这份规划本身不等于启动外部会话或付费资源的指令。

### 6.3 领取、回传、整合

工作者启动先确认分配仍属于自己，不改全局 tasks/current/handoff。完成后回传任务 ID、基线提交、产物提交（没有则说明）、修改文件、验收证据、已知限制、契约变更请求。

负责人检查 diff、重复/越权修改、测试和依赖一致性，顺序整合，再更新 tasks 与 changelog。工作者说“完成”只变成 REVIEW；只有整合后验收通过才变成 DONE。

主工作区保留用户已有改动；不得使用 `git reset --hard`、覆盖 checkout、强推或删除其他工作者 worktree 来解决冲突。push/发布/真实平台写入按用户授权处理，不由“完成任务”自动推导。

### 6.4 中断后的所有权

会话消失不意味着它的任务可重领。负责人先检查 worktree、提交、dirty diff 和工作者状态，记录已接管/释放所有权后重新分配。未整合成果留在 handoff 的“待整合”表，不能先写进 changelog 当成发布成果。

## 7. 测试与验收规范

### 7.1 必测行为

去重与冷却边界；最新欢迎候选替换；TTL；插播恢复源位置；模式互斥；目录数字排序与独立洗牌袋；n-1 商品切换；30 秒续显与并发换品；旧响应/旧引擎结果隔离；暂停、停止、断线、恢复；关键词多规则竞争；标记注入防护。

使用 FakeClock/种子随机/模拟事件驱动竞争状态，不用真实等待 30 秒或睡眠 4 小时做单元测试。端到端长跑测试另设显式任务，避免混入日常测试。

### 7.2 三层证据

| 层级 | 能说明什么 | 不能说明什么 |
|---|---|---|
| 单元/模拟 | 规则与调度在受控输入下正确 | TikTok 当前可接入 |
| 本地集成 | 真实 TTS 可用、音频设备可播放和恢复 | 平台展示/发送成功 |
| 平台实测 | 授权直播间实际收到事件和执行动作 | 所有账号/地区永远支持或事件100%完整 |

新增有实质行为的模块要有覆盖该行为的测试；不为低影响文档改动编造单元测试。对本切片运行最小相关检查；整合再做依赖相关回归；无新变化/疑虑不反复重跑全套。

完成实现时记录精确命令、退出结果和实测限制。待创建工具链的建议命令不是已执行证据。编译通过不能代替功能验收。外部测试默认关闭，需要明确配置测试房间和许可，不能在普通 `dotnet test` 中发直播文字或展示商品。

## 8. 决策边界

| 可自主决定并记录 | 需用户或整合负责人先决定 |
|---|---|
| 已批准切片内的类拆分、命名、错误提示、测试用例 | 修改用户需求、默认业务语义或验收范围 |
| 常规可逆修复与相关验证 | 更换已冻结共享接口、技术栈、迁移顺序：由负责人统筹 |
| 未明确数值的开发默认值，必须在需求中标“暂定” | 新增云端 TTS/LLM、账户体系、平台或收费功能 |
| 只读检查工具链与仓库 | 真实直播发言、商品操作、使用凭据/账户：需要已有明确授权 |
| 模拟输入、故障注入、本地测试 | 付费调用、大模型下载、驱动安装、部署/发布：核对具体授权 |

阻塞报告必须包含：阻塞 Task ID、已确认事实、无法继续的具体动作、已尝试的安全替代、所缺的最小输入。平台某能力阻塞时可继续当前切片已分配的独立本地任务，不能另开未经分配的新功能。

## 9. 六文档维护规则

- requirements：只记产品事实、业务规则、默认值和待验证能力；改动带版本和原因。
- agents：只记稳定开发规则；不要夹带临时错误日志。
- tasks：所有任务的依赖、优先级、状态、所有权和验收链接。
- current_task：唯一当前切片，必须明确允许/禁止修改路径与停止点。
- handoff：当前真实工作区、未完成/未整合、验证证据和下一条动作；接近中断时优先更新。
- changelog：只追加已经形成可集成成果的记录；实现中、未测试、未整合内容不冒充完成。

六文件中的 Task ID、文档版本、当前切片、完成状态必须一致。状态更新由负责人单独提交或与相应整合提交一起保存。

每轮结束必须说明：完成了什么、影响哪些模块、验证证据、未完成什么、下一项允许动作。任务完成不等于用户需求全部交付，不能在缺少真实接入/TTS证据时说“可以商用上线”。
