# TikTok PC 直播音频工具：产品需求与业务规则

文档版本：0.1.0｜日期：2026-09-16｜目标系统：Windows 10/11 x64｜阶段：需求基线，尚未实施。

本文件是六份文档中唯一的产品需求真相。用户后续明确指示可以修改本文件；实现便利、第三方软件行为和 AI 推测不能自行改变需求。本文中“必须”为验收条件，“默认”为可由用户修改的初始值，“待验证”为尚未证实的外部能力。

## 1. 产品目标与范围

开发一款 PC 桌面音频工具，在 TikTok 直播时播放越南语直播稿或预制音频，接收直播事件，优先插播欢迎、关注感谢、评论关键词回复，再恢复原播放位置；同时控制当前商品讲解链接、定时文字和关键词文字回复。

第一版只支持一个正在工作的直播间、一个事件来源、一条主语音输出。视频合成、视频变换和推流交给外部 OBS 或 TikTok LIVE Studio，本项目不改造 OBS，也不开发视频工具。

需求来源的优先顺序：用户最新明确要求 → 用户此前未被修改的明确要求 → 本文的暂定实现默认值。前面对“播个够”的检查仅用于功能参考，不作为其实际频率、平台权限或算法效果的证明。

### 1.1 已确定的用户要求

- 监控评论、关注、点赞、进入直播间用户，来源可以是 TikTok PC 直播工具或直播网页。
- 基础语音二选一：TXT 经本地越南语 TTS 转音频，或者分组预制音频。
- 欢迎、关注感谢、评论关键词回复分别配置 K、V、W 秒冷却；事件去重；互动语音优先插播，然后恢复基础语音。
- 预制音频按产品编号、子组编号顺序推进，每个子组随机选一段，整个产品列表无限循环。
- 播放 n-1 时展示商品链接 n；同一产品还未结束时，按 30 秒规则续显；切换产品时改为新产品链接。
- 话术标记 `@[3]` 表示展示链接 3；支持手动、顺序及循环场控。
- 本地 TTS 引擎可切换；支持定时文字、评论关键词文字回复和关键词库维护。
- 音频支持可开关的随机声道、噪声/环境音、音效、语速等变化。

### 1.2 V1 不包含的功能

多平台、多账号并播、礼物答谢、自由生成式问答、云端 TTS、云端账户/收费授权/充值后台、数字人、视频动态处理、独立编码推流、自研声卡驱动、修改或嵌入 libobs、移动 App 均不在 V1 内。未来增加时必须先取得用户明确需求并修改本文和任务索引。

本版“AI 语音助手”特指规则生成文字后用本地 TTS 合成；固定回复不需要接入大语言模型。用户更早提及的自由 AI 问答留作后续候选，不能悄悄加入本版。

## 2. 概念、模式与状态

| 名称 | 定义 |
|---|---|
| 产品 n | 由本地稳定 ProductId 标识的一组讲解素材；编号 n 用于排序和界面显示 |
| 音频组 n-m | 产品 n 下第 m 个讲解环节；每个产品可以有不同的组数 M(n) |
| 基础播放 | 直播稿顺序播放或预制音频顺序编排，两者互斥 |
| 互动插播 | 欢迎、关注感谢或关键词回复语音；与基础语音不会同时说话 |
| 商品弹窗/续显 | 在 TikTok 直播间展示、重新展示商品讲解卡/链接；不是本机通知窗口或打开浏览器标签页 |
| 当前讲解产品 | 基础播放流程已经进入的产品，互动插播期间保持不变 |
| 当前展示链接 | 场控控制的链接目标；可被 `@[n]` 或手动操作覆盖 |
| SessionId | 每次用户开始一场本工具直播会话时创建；网络重连不改变，明确结束再开始才改变 |

播放状态：`Idle / Preparing / BasePlaying / InterruptPlaying / Paused / Stopped / Error`。
事件源状态：`Disconnected / Connecting / Connected / NeedsLogin / Unsupported / Error`。
场控状态：`Disabled / Ready / Pending / NeedsConfirmation / Suspended / Unsupported / Error`。
三类状态分别维护。某个网页能力不可用，不应误报音频引擎本身故障。

## 3. TikTok 事件与场控接入

### REQ-SRC-001 可替换的事件适配器

通过 `ILiveEventSource` 统一接入 `Enter / Follow / Like / Comment / RoomStatus`。PC 工具和网页只是候选来源，不表示已经有可用官方 SDK。首期只选一个经过验证的来源，不同时采集两套重复事件。

每个事件至少包含：`SourceId, SessionId, RoomId, EventType, EventId?, UserId?, DisplayName?, OccurredAt?, ReceivedAt, Content?`。时间统一保存 UTC；业务计时使用可注入的单调时钟。点赞附带数量时保留增量/累计语义，不混淆二者。

事件日志必须区分“收到”“因去重忽略”“因冷却忽略/待处理”“进入语音队列”“播放成功”“失败”。点赞 V1 只监控和展示，不自动生成点赞感谢语音。

### REQ-SRC-002 能力声明与真实验证

适配器独立报告 `ReadEnter, ReadFollow, ReadLike, ReadComment, ReadRoomStatus, ShowProduct, ReadProductVisibility, SendText`。不支持的能力须禁用对应 UI 并说明原因，不能返回伪成功。

事件读取权限不等于商品操作或文字发送权限；接收评论不等于能发评论。显示昵称不等于获得稳定用户标识。公开页面也不保证能获得每一名进场或点赞用户。针对性互动仅对实际收到且可识别的事件生效；无法验证完整性时必须标记覆盖范围未知。

首轮验证必须在用户授权的直播间/测试条件下记录每项能力的真实证据、登录要求、平台限制和断线行为。没有直播条件时可以完成模拟链路，不能把模拟验收写成 TikTok 已接通。

### REQ-SRC-003 连接异常

断线期间继续本地基础音频，暂停自动商品操作和自动发文字；停止处理过期互动。重连保留 SessionId 和去重记录，并重新核对房间及当前商品。登录失效进入 NeedsLogin，提示人工恢复，不反复重启或自动绕过验证码。

## 4. 去重、冷却与队列

### REQ-EVT-001 两层去重

1. 事件去重：有 EventId 时使用 `SourceId + RoomId + EventType + EventId`。断线重放同一事件不再触发副作用。
2. 业务去重：欢迎和关注感谢分别使用 `SessionId + RoomId + UserId + ActionType`，同一用户每场各成功播报最多一次。

评论按评论事件去重，不能将用户所有后续提问都当成重复；不同 EventId 的新评论可重新匹配规则，但仍受 W 和规则冷却控制。

缺少 EventId 时使用事件类型、用户标识、规范化内容、可用源时间等生成短期指纹并进行容差匹配。它可能误合并或漏掉事件，必须显式标记为降级，不声称严格去重。缺少稳定 UserId 时默认跳过针对性欢迎/关注，保留日志；不得仅靠可重名昵称承诺“一人一次”。

接收去重记录与播报成功记录分离。等待队列中的用户处于 Reserved；播放完整结束才标记 Completed。被丢弃、取消或合成失败不算已成功欢迎，但同一事件不会无限重试。

执行副作用前持久化事件去重与动作占位。崩溃时，若某条语音已开始但无法确认是否完整结束，恢复为 Unknown，不自动重播，也不写成成功；同场对应用户占位保留，需人工确认后才能释放。这样避免恢复时重复欢迎，同时如实保留“可能已经播过”的不确定性。

### REQ-EVT-002 冷却语义

K、V、W 分别是该类别全局冷却，单位秒，从对应互动语音完整播放结束开始；不是按人数触发。冷却中不立即播放同类下一条。三类冷却独立，单场用户去重额外生效。

暂定默认值为 K=15、V=10、W=10，允许 0–3600 秒；0 表示不加此类别时间冷却，但仍保留事件/用户去重。默认值用于开发，不是从“播个够”推算的实测参数。

关键词规则可额外设置冷却；需要类别 W 和规则冷却同时通过。文字发送采用独立冷却，见 REQ-CTRL-004。

### REQ-EVT-003 有界等待与新鲜度

| 类别 | 等待策略 | 暂定 TTL |
|---|---|---|
| 欢迎 | 尚未播放的候选最多 1 名；新的合格进场用户替换旧候选，冷却结束只处理仍新鲜的最新候选 | 20 秒 |
| 关注 | 按接收顺序排队，最多 20 名；满时丢弃最旧的未播放条目，并记录 | 60 秒 |
| 关键词语音 | 合格评论候选最多 1 条；保留最新匹配评论 | 30 秒 |

TTL 从 ReceivedAt 计算，合成前及播放前都检查；过期、房间变化、场次变化的任务直接取消。欢迎“最新”按 OccurredAt 排序，缺失时使用 ReceivedAt。已开始播放的互动不被新候选替换。

优先级默认：人工紧急停止 > 关注感谢 > 关键词回复 > 欢迎 > 基础播放。互动语音之间不互相打断，优先级用于选择下一条，避免多个声音重叠。任何重试/重连不能造成无界队列。

## 5. 互动语音规则

### REQ-INT-001 欢迎

收到有效进场事件，经过去重、新鲜度和 K 检查，生成欢迎语并本地合成。音频准备好后暂停基础语音，立即优先插播；结束后从基础语音原位置恢复。

中文语义模板：`欢迎{触发用户}进入直播间`。
默认越南语模板：`Chào mừng {userName} đến với buổi livestream!`

### REQ-INT-002 关注感谢

收到有效关注事件，经过去重和 V 检查，准备感谢语音，然后暂停当前基础播放，播放感谢，恢复原位置。这里用户要求的“停止原来的音频”实现为暂停并保存游标，不是丢弃当前片段。

中文语义模板：`感谢{触发用户}的关注`。
默认越南语模板：`Cảm ơn {userName} đã theo dõi!`

### REQ-INT-003 评论关键词语音

只处理新评论事件；匹配启用的关键词规则，经过 W、规则冷却与 TTL 检查后合成并优先插播。没有匹配则不回复，不自动扩展成自由 AI 回答。

中文语义模板：`{触发用户}，麻烦先点关注，有问题客服都能给你完美解决`。
可编辑的默认越南语表达：`{userName} ơi, bạn hãy nhấn theo dõi nhé. Nếu có câu hỏi, bộ phận chăm sóc khách hàng sẽ hỗ trợ bạn.`

越南语模板保留“请关注、客服协助”的用途，实际措辞与发音需用户试听验收。不能直接把中文模板交给越南语引擎并声称已完成越南语支持。

### REQ-INT-004 模板与昵称

支持多条候选模板随机选取、预览和试听。昵称保留越南语变音符号，过滤控制字符、标记和不可朗读的装饰字符；超长昵称截短或使用用户设置的通称。用户昵称和评论中的 `@[n]` 绝不能触发商品操作。控制标记只允许出现在本机操作者编辑的稿件/元数据中。

固定规则优先级最高者获胜；同优先级按明确规则顺序，仍相同时按稳定 RuleId。V1 支持精确匹配、包含匹配；越南语默认保留重音，不擅自将不同单词视为相同。多个关键词可配置任一/全部匹配。正则留至明确需要时再增加。

## 6. 音频调度与恢复

### REQ-AUD-001 唯一主语音输出

任何时刻最多一条主语音：基础或互动。环境音/BGM 可独立混音，但不得形成基础讲解与互动两条人声同时播放。基础模式只能激活一个；切换模式须通过控制台的切换操作取消旧计划，不能并行叠播。

### REQ-AUD-002 准备后插播

接收事件 → 判定 → 准备/合成互动音频（基础继续播放）→ 再检查资格 → 保存基础游标 → 短淡出并暂停基础 → 播放互动 → 再选合格互动或恢复基础。

准备失败不得先把基础音频停掉。关注、欢迎、评论均默认准备好即插播，不等待整段基础音频结束。互动播放失败也必须尝试恢复基础，不能永久卡在 InterruptPlaying。

### REQ-AUD-003 游标不推进

保存 `Mode, PlanRevision, ProductId?, GroupId?, ClipId/SegmentId, SourceSampleOffset, Cycle, EffectSeed`。插播不抽取新随机音频、不跳组、不推进产品、不重新执行已经消费的 `@[n]` 标记。

以解码后的源采样位置恢复，随机速度不会让恢复位置漂移。不可精确定位的文件先转成可定位的本地缓存再允许加入播放列表，不默认重头播放长片段。

### REQ-AUD-004 停止、暂停与故障

“暂停全部”暂停主语音、商品续显和定时文字；恢复时核对房间与当前链接，然后重新开始续显计时。“停止”清空当前会话待执行副作用；恢复工作必须由用户重新开始。紧急停止应在 500ms 内停止本机语音并阻止新外部操作。

程序异常退出后从持久化检查点提供恢复预览，绝不自动发音、发文字或操作商品。未确认的外部动作不自动重放。

## 7. 本地越南语 TTS

### REQ-TTS-001 本地引擎与切换

TTS 必须调用用户机器上的音频引擎。首版采用本机 loopback HTTP 服务适配接口（127.0.0.1/localhost），不自动回退到云端；具体引擎、音色和模型在引擎验证任务中确定。新增本地进程方式需独立适配器，禁止散落在 UI 中调用。

支持配置多个引擎实例，并对当前引擎进行健康检查、列出/配置音色、越南语样句试听。界面展示实际引擎与模型版本。引擎不可用时提示修复或切换；预制基础音频可继续，无法合成的互动按 TTL 丢弃。

首次只实现一个实测可用引擎；以第二个真实引擎验证“可切换”作为完整验收条件。两个模拟适配器只算接口测试，不能算引擎切换交付。

### REQ-TTS-002 TXT 处理

导入 UTF-8（含 BOM）TXT；其他编码提示重新选择编码。显示原文和分段预览，按段落、句子和引擎长度限制切分，保留越南语重音。支持本地生成、播放、缓存、重试、取消、试听和播放列表管理。

TTS 稿默认顺序播放并在结束后循环，可切换只播一次。至少准备好当前及下一段后开始；合成速度赶不上播放时明确显示等待合成，不伪造连续语音。预生成足够音频后可以离线播放。

### REQ-TTS-003 缓存与确定性

缓存键包含文本、引擎/模型标识及版本、音色、语言、语速/音调/音量、分段器和发音词典版本。先写临时音频，验证格式和可解码后原子提交，失败文件不能成为命中缓存。

切换引擎：当前正在播放的段落/互动使用已生成音频完成；尚未开始的段落和互动改由新引擎生成，旧引擎未完成任务取消或忽略其过期结果。新引擎准备期间原基础音频正常继续。以 EngineRevision 阻止旧结果覆盖新队列。

被互动暂时打断的基础片段仍属于“已经开始”，恢复时继续原音频和游标，不因引擎切换重新合成它。只有下一条尚未开始的内容使用新引擎。

### REQ-TTS-004 TTS 中的商品标记

仅解析操作员稿件中的 `@[n]`；标记本身不送给 TTS、不朗读。将标记切成确定的播放边界，在其后第一段语音真正开始时切换商品目标并弹窗，随后续显当前链接。标记之前没有产品目标时不弹窗。插播后从中途恢复不再触发旧标记。

连续标记且中间无可播放文字时，只执行最后一个目标；全文末尾没有后续语音的标记在导入预检查中提示，不单独触发弹窗。无效编号或未映射产品必须在开始前修正。

## 8. 预制音频编排与音效

### REQ-PRE-001 目录与导入

示例：

```text
Products/
  1/
    1/  a.wav, b.mp3
    2/  a.wav, b.wav
  2/
    1/  a.wav
    2/  a.wav, b.wav
    3/  a.wav
```

仅导入指定目录中的媒体与操作者元数据，不扫描整个磁盘。产品和子组按数字排序，10 在 2 后；支持每个产品不同组数。空目录、重复编号、不支持/损坏文件和缺失链接映射在预检查时集中列出；默认阻止有问题的产品开始，不能静默跳到错误商品。

预制音频的可选商品覆盖通过操作员在素材配置中设置“片段开始时展示产品 n”，作为元数据保存；不识别音频里念出的字符，也不把文件名任意解释为命令。若需要片段中间换品，先由操作员拆成多个片段，再设置各片段边界。

### REQ-PRE-002 顺序与随机

`1-1 → 1-2 → … → 1-M(1) → 2-1 → … → N-M(N) → 1-1`。

每进入子组抽取一段播放。每个子组独立维护洗牌袋：该组的所有可用片段被选完才重新洗牌，有至少两段时避免跨袋连续相同。仅一段时允许重复。不把多个碎片随机跨组拼接。每次整轮回到 1-1 都是新的产品进入操作。

### REQ-PRE-003 音效变化

在片段开始时一次性采样并固定效果参数，插播恢复保持相同参数。允许启用轻微速度变化、音调、声道平衡、EQ 和可选环境音/噪声混合。参数范围可配置并支持效果试听、旁路和削波保护。

初始默认效果关闭；推荐试音预设速度 0.97–1.03 倍、音调 ±0.3 半音、平衡 ±0.1、环境音相对主语音不高于 -35dB。速度变化优先保留音调。实际阈值由试听决定，随机变化不承诺“无限不同”或平台检测结果。禁止因效果丢失越南语可懂度或产生爆音。

## 9. 商品链接弹窗与续显（本次关键修订）

### REQ-CTRL-001 产品绑定

本地 ProductId/编号 n 必须显式映射到本场直播平台商品标识；平台列表的当前位置可能变化，不能直接把“第 n 行”当作永久商品 ID。连接房间或刷新商品列表后需验证映射。

预制模式：n-1 选中片段真正开始播放时提交“进入产品 n”事件，展示链接 n。n-2 至 n-M 不重新切换目标；互动欢迎/感谢不切换目标。

### REQ-CTRL-002 30 秒续显

“平台约 30 秒后自动隐藏”是用户给定业务规则，尚未实测；实现默认续显周期 30 秒，后续按所选接入渠道测试校准。计时从最后一次确认展示成功开始，不从发起请求开始。

只要会话运行、场控启用且当前目标未变，到期后重新展示同一链接。一次确认成功更新 LastShownAt 并重排下一次续显，不使用无限堆积的固定定时请求。网络和平台处理会有延迟，不能保证第 30 秒零间隙；若实测需要提前续显或平台不接受续显，先报告并由用户确认策略。

例：产品 1 于 0 秒开始，持续讲解 80 秒：0 秒展示 1，约 30 秒再展示 1，约 60 秒再展示 1，80 秒进入产品 2 立即展示 2，下一次在其成功约 30 秒后续显 2。原计划在 90 秒的产品 1 续显必须失效。

### REQ-CTRL-003 切换、覆盖与并发

- 所有商品操作经过唯一串行控制器；当前目标变化时递增 ProductEpoch，取消旧定时任务和未提交的旧请求。
- 每次请求提交前核对 SessionId、RoomId、目标及 ProductEpoch；旧响应不得更新新目标的成功时间。平台已接受且无法撤销的旧请求不能保证取消，需记录并在通道可用时纠正为最新目标。
- `@[n]`、手动选择会覆盖当前展示链接并开始续显，直到下一次显式标记/手动选择或基础播放进入新产品。覆写不改变基础音频游标。
- 同时发生时优先级：停止/房间变化 > 人工操作 > 操作者稿件标记 > 产品进入 > 定时续显。同一播放边界中的显式标记优先于目录隐式编号。
- 联动模式由音频决定目标，是默认模式；独立顺序/循环场控是互斥模式，不能与音频联动计时器同时控制链接。独立模式使用操作员配置的商品顺序和单品停留时长，默认 30 秒，实际下限服从已验证的平台限制；停留超过 30 秒时仍对当前目标续显。顺序一次模式结束后停止自动展示，循环模式返回第一项。
- 独立模式忽略基础音频的产品进入和稿件标记，避免两套计划争抢；手动选择暂停独立顺序推进并续显手选目标，操作员点击“恢复顺序”才从下一计划项继续。界面必须显示当前控制模式。
- 暂停全部、停止、结束直播、断开源、切换房间时，取消续显和待处理旧操作。互动插播期间当前产品不变，续显继续。
- 暂定请求超时 5 秒；只有明确失败且允许重试时最多重试 1 次，间隔 2 秒。结果未知时先读取平台状态；不能读取则进入 NeedsConfirmation，停止盲目重复请求。不能把“点击成功”写为“平台已展示”。

### REQ-CTRL-004 定时文字与关键词文字

支持定时文字任务、多条话术顺序/随机、时间段、启停和发送记录。默认发送总间隔 30 秒（可配置，且不低于已验证的平台限制）；手动、定时和关键词文字共用单个发送通道。

关键词规则提供独立 `EnableVoiceReply / EnableTextReply / VoiceTemplates / TextTemplates`；可以两者同时开启，每种各执行最多一次，不能因监听重复而重复发送。文字不占主音频队列；文字冷却不启动或重置 K/V/W。

没有可用且启用的规则时不发送，过期评论不补发。超时结果未知时不盲目重发；若平台不支持验证发送结果，记录 Unknown。定时任务重连后只安排未来时点，不补发断线期间的全部消息。

## 10. 控制台与数据

### REQ-UI-001 必需界面

连接与能力状态；基础模式选择；音频设备/电平/试听；当前产品/组/片段/进度；当前展示链接/最后成功时间/下次续显倒计时；互动候选/播放队列；K/V/W；关键词库；本地 TTS 引擎配置/测试/切换；产品目录与平台商品映射；定时文字；日志与异常；开始、暂停全部、恢复、停止、紧急停止。

只有真实能力验证通过才允许开启相应自动场控。首次启用外部自动操作需操作员在 UI 明确开启，并可随时独立关闭。

模拟模式允许预览同样的操作流程，但必须显著显示“模拟，不操作真实直播间”，且适配器不得连接真实平台写入通道。模拟确认不能解除真实能力的禁用状态。

### REQ-DATA-001 持久化

SQLite 保存设置、规则、产品/组及素材索引、播放检查点、洗牌袋、SessionId、事件指纹、互动完成记录、商品展示状态和动作记录。音频缓存保存在本地文件目录，数据库不直接保存大音频。

用户数据默认位于 `%LOCALAPPDATA%/<最终产品名>/`，开发模式使用项目忽略的 `.local-data/`。凭证放 Windows 安全存储，不写入仓库、导出包和普通日志。日志有大小轮转及清理入口，不默认长期保存完整评论/个人信息。

## 11. 验收与证据层级

验收分为：模拟/单元验证、本地真实引擎和设备、TikTok 真实房间三层。完整产品验收必须三层各自通过；某层不能进行时明确阻塞，不用上一层代替。

| 编号 | 验收场景 |
|---|---|
| AC-01 | 同一事件重放、重连和进程恢复后不重复播报；同场同用户欢迎/关注各最多成功一次 |
| AC-02 | K/V/W 从播报结束计时；欢迎只保留最新有效用户；过期候选、未命中关键词均不插播 |
| AC-03 | 慢/失败的 TTS 不先暂停基础音频；准备好后本机抢占调度延迟目标 ≤500ms；不会两条主语音叠播 |
| AC-04 | 插播后准确恢复当前片段源位置，误差目标 ≤100ms；随机选择、组号、产品号及已触发标记不变 |
| AC-05 | 1→2→10 数字排序正确，每组一次抽样，每组洗牌袋不提前重复，M(n) 不同时仍完整循环 |
| AC-06 | 本地真实越南语 TXT 合成可播放，重音/昵称样例试听通过；断网不调用云端；两个真实引擎可切换 |
| AC-07 | 产品1于0/30/60秒展示、80秒切产品2，90秒绝不提交产品1续显；插播期间仍续显当前商品 |
| AC-08 | 模拟旧请求延迟完成、30秒边界恰好换品、网络结果未知、标记覆写；均遵守最新目标和不盲重试规则 |
| AC-09 | TXT `@[n]` 只在相应语音边界触发一次且不朗读；用户评论和昵称中的同类标记不会操作商品 |
| AC-10 | 文字和语音可单独或同时启用；每评论每渠道至多一次，发送总间隔生效，断线不补发洪峰 |
| AC-11 | 暂停/停止/房间变化取消未来副作用；崩溃恢复先显示预览，不自行发音/操作平台 |
| AC-12 | 本地目标机器 4 小时持续播放与事件负载测试无崩溃、无无界队列；记录内存、音频中断和错误，不预写通过 |
| AC-13 | TikTok 四类事件、商品展示/续显、文字发送分别提供真实证据；缺少某项只能宣称对应部分可用 |

TTS 从事件到音频就绪的性能需在选定硬件/引擎上记录中位数和 P95，暂不承诺统一秒数。平台动作延迟与失败率也必须实测。

## 12. 尚需选择/验证的事项

| 项目 | 状态与处理方式 |
|---|---|
| 首个事件来源、PC 工具实际可读能力 | T030 验证后选择；无证据前仅模拟 |
| 稳定用户 ID、事件完整度、商品控制权限、30秒实际行为 | T031/T032 分项验证；无法满足则报告能力缺口 |
| 本地越南语引擎、模型与第二引擎 | T040/T044 用目标机器试听及测试决定；未经授权不下载大型模型或付费 |
| 越南语音色/语气、昵称读法 | 可编辑模板和样例试听，由用户确认质量 |
| 产品链接标识与目标账号 | 实接入阶段需要用户提供或在授权账号内读取 |
| 产品名称与硬件配置 | 先使用内部代号；不影响领域逻辑，不据此扩张功能 |

这些未决项不阻止可独立验证的本地开发，但阻止对应真实能力的完成声明。
